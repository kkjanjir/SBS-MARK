1. **Extract pure functions:** Move `getDynamicSubjects` and `getGrade` outside the `MarksheetApp` component. These functions do not depend on component state and moving them out prevents their recreation on every render, which is a common source of unnecessary re-renders.
2. **Memoize helper functions:** Wrap `getClassSubjects`, `getCalculations`, and `getClassRank` in `useCallback` to ensure they maintain referential equality across renders unless their specific dependencies change. This is critical because they are used to compute props for heavily rendered components.
3. **Memoize the bulk print container:** The `<div id="print-bulk-container">` contains a large list of `MarksheetTemplate` components that render even when hidden via CSS (`display: none`). Wrap the mapping operation inside a `useMemo` block, assigning the result to a `bulkPrintContent` variable. This will prevent O(N) re-renders of the print container during unrelated state changes (like typing in the search bar or changing tabs).
4. **Remove duplicated `getGrade`:** Remove the inline `getGrade` function from inside the `MarksheetTemplate` component, as it will use the globally extracted pure function instead.
5. **Verify type safety and changes:** Run a targeted TypeScript check using the provided command to ensure no imports or types were broken, then visually verify the code diff.
6. **Complete pre-commit steps:** Follow instructions to ensure testing and verifications are done.
7. **Submit the PR:** Create a PR with the '⚡ Bolt: [performance improvement]' title format detailing the memoization and pure function extraction.
